<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Valid_Consent__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(Expiry_Date__c &gt; TODAY() +30, Consent_Signed__c = TRUE, Consent_Attached__c = TRUE), TRUE, FALSE)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This field will only fill when consent signed and consent attached are ticked and validated.</inlineHelpText>
    <label>Valid Consent</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
